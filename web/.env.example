# Point the frontend at a Bedrock explanation backend (optional).
# Leave unset to run fully local with the deterministic engine only.
#
# Local backend:   VITE_API_URL=http://localhost:8787
# Deployed backend: use the CDK "ApiFunctionUrl" output, or rely on the
#                   runtime /config.json written automatically by the CDK deploy.
# VITE_API_URL=http://localhost:8787
